// import CreationPage from "./creationPage.js";

class PlayGroundCreationPage extends CreationPage {
    constructor(number) {
        super(number);
    }

    buildSkeleton(id) {
        return super.buildSkeleton(id);
    }
}

// export default PlayGroundCreationPage;